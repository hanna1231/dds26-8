---
phase: 13-integration-benchmark
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified: []
autonomous: false
requirements: [INT-01, INT-02, INT-03]

must_haves:
  truths:
    - "SAGA/gRPC mode passes integration tests"
    - "SAGA/queue mode passes integration tests"
    - "2PC/gRPC mode passes integration tests"
    - "2PC/queue mode passes integration tests"
    - "Kill-test produces 0 consistency violations in 2PC mode"
    - "Benchmark produces 0 consistency violations in all 4 modes"
  artifacts: []
  key_links:
    - from: "docker-compose.yml"
      to: "stock/app.py, payment/app.py, orchestrator/app.py"
      via: "COMM_MODE and TRANSACTION_PATTERN env vars"
      pattern: "COMM_MODE"
---

<objective>
Validate all 4 mode combinations (SAGA/2PC x gRPC/queue) with Docker integration tests, kill-tests, and benchmark.

Purpose: This is the final validation gate for v2.0. Every prior phase built individual pieces -- this phase proves they work together under normal operation, container failure, and benchmark load.

Output: Verified passing results for all 4 modes across integration tests (INT-01), kill-tests (INT-02), and benchmark (INT-03).
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-integration-benchmark/13-RESEARCH.md

@.planning/phases/13-integration-benchmark/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run 4-mode integration tests and fix any failures</name>
  <files></files>
  <action>
Run integration tests across all 4 mode combinations using Docker Compose. For each mode:

1. Clean previous state: `docker compose --profile simple --profile full down -v --remove-orphans`
2. Start cluster with env vars:
   ```bash
   TRANSACTION_PATTERN={tp} COMM_MODE={cm} SAGA_STALENESS_SECONDS=10 \
   ORDER_REDIS_HOST=shared-redis-0 STOCK_REDIS_HOST=shared-redis-0 \
   PAYMENT_REDIS_HOST=shared-redis-0 ORCH_REDIS_HOST=shared-redis-0 \
   docker compose --profile simple up -d --build
   ```
3. Wait 20 seconds for Redis cluster init and service startup.
4. Run: `cd test && python -m pytest test_microservices.py -v`
5. Record pass/fail for each mode.

Test all 4 combinations:
- TRANSACTION_PATTERN=saga COMM_MODE=grpc (baseline, should pass)
- TRANSACTION_PATTERN=saga COMM_MODE=queue
- TRANSACTION_PATTERN=2pc COMM_MODE=grpc
- TRANSACTION_PATTERN=2pc COMM_MODE=queue

If any mode fails:
- Check docker logs: `docker compose logs {service-name} --tail 50`
- Fix the issue in the relevant source file
- Re-test that mode

IMPORTANT: After running all 4 modes, do a final `docker compose --profile simple down -v` to clean up.

Anti-patterns to watch for (from RESEARCH.md):
- Queue mode timeout = queue consumer not started (check stock/payment logs for "queue_consumer started")
- NOGROUP error = setup_command_consumer_group not called (check startup sequence)
- Stale Docker image = forgot --build flag (always use --build)
  </action>
  <verify>
    <automated>cd /Users/daniel/WebstormProjects/dds26-8 && echo "Integration tests must pass in all 4 modes via manual Docker Compose execution"</automated>
  </verify>
  <done>All 4 mode combinations (saga/grpc, saga/queue, 2pc/grpc, 2pc/queue) pass test/test_microservices.py integration tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify kill-test and benchmark across all modes</name>
  <action>
Human verifies kill-test consistency (INT-02) and benchmark results (INT-03) across all 4 mode combinations. Claude has already run the integration tests in Task 1.
  </action>
  <verify>User confirms 0 consistency violations in kill-test and benchmark</verify>
  <done>Kill-test and benchmark pass with 0 consistency violations in all tested modes</done>
  <what-built>4-mode integration test suite, kill-test consistency, and benchmark validation</what-built>
  <how-to-verify>
1. Review Task 1 output: confirm all 4 modes passed integration tests
2. Run kill-test for 2PC mode (INT-02):
   ```bash
   make kill-test-all-modes
   ```
   Or manually for just 2PC/gRPC:
   ```bash
   TRANSACTION_PATTERN=2pc COMM_MODE=grpc make kill-test SERVICE=stock-service
   ```
3. Run benchmark (INT-03):
   ```bash
   make benchmark-all-modes
   ```
   Or manually for a single mode:
   ```bash
   TRANSACTION_PATTERN=saga COMM_MODE=grpc make dev-clean dev-up && sleep 20 && make benchmark
   ```
4. Verify: 0 consistency violations in all cases (no lost money, no lost items)
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
1. All 4 mode combinations pass `test/test_microservices.py`
2. Kill-test: 0 consistency violations (credits_deducted == stock_consumed) in 2PC mode
3. Benchmark: 0 consistency violations in all 4 modes (wdm-project-benchmark consistency test)
</verification>

<success_criteria>
- INT-01: All 4 mode combinations (SAGA/gRPC, SAGA/queue, 2PC/gRPC, 2PC/queue) pass Docker integration tests
- INT-02: Kill-test produces 0 consistency violations in 2PC mode (no lost money or items after crash recovery)
- INT-03: wdm-project-benchmark consistency test passes with 0 violations in all 4 modes
</success_criteria>

<output>
After completion, create `.planning/phases/13-integration-benchmark/13-02-SUMMARY.md`
</output>
